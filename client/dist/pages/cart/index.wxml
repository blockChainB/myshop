<block wx:if="{{$taroCompReady}}">
    <view class="cart">
        <scroll-view class="{{cartClass}}" scroll-y="{{!showEidtBox}}" bindscroll="onViewScroll" enable-back-to-top="{{true}}" data-e-scroll-so="this">
            <commondity compid="{{$compid__61}}"></commondity>
            <goods compid="{{$compid__62}}"></goods>
        </scroll-view>
        <block wx:if="{{showEidtBox}}">
            <edit-box compid="{{$compid__63}}"></edit-box>
        </block>
        <view class="cart_bottom">
            <bottom-bar compid="{{$compid__64}}"></bottom-bar>
        </view>
    </view>
</block>