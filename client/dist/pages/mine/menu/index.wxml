<block wx:if="{{$taroCompReady}}">
    <view class="user-menu">
        <view wx:key="$original.key" class="{{menu.$loopState__temp2}}" bindtap="handleClick" wx:for="{{loopArray12}}" wx:for-item="menu" wx:for-index="index" data-e-tap-so="this" data-e-tap-a-a="{{menu.$original}}">
            <image class="user-menu__item-img" src="{{menu.$original.img}}"></image><text class="user-menu__item-txt">{{menu.$original.text}}</text>
        </view>
        <button open-type="contact" bindcontact="handleContact">联系客服</button>
    </view>
</block>